@page "/inventory/packages/{ItemId:long}"

<InventoryMenu />

@if (Item != null)
{
    <div class="m-main-content">
        <InventoryPackageEditor Item="Item" OnValidSubmit="Save" OnDiscard="Discard" />
    </div>
}