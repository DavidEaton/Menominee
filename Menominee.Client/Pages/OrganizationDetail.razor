@if (Organization != null)
{
    <section class="pl-5 pr-2">
        <div class="d-flex justify-content-between">
            <h4 class="page-item">@Organization.Name</h4>
            <div @onclick="Close"><span class="oi oi-x" aria-hidden="true"></span></div>
        </div>
        <EditForm Model="@Organization" OnValidSubmit="HandleValidSubmit" OnInvalidSubmit="HandleInvalidSubmit">

            @if (FormMode == FormMode.Read)
            {
                <div class="form-group row">
                    <label for="name" class="col-sm-3">Name: </label>
                    <label id="name" class="col-sm-8 editable">@Organization.Name</label>
                </div>
            }

            @if (FormMode == FormMode.Edit)
            {
                <OrganizationNameForm @ref="OrganizationNameForm"
                                      CloseEventCallback="@OrganizationNameForm_OnOrganizationNameChanged">
                </OrganizationNameForm>
            }


            @*<div class="form-group">
                <label class="col-sm-6 control-label" style="text-align: left;font-size: 14px;">
                    Name
                </label>
                <SfInPlaceEditor
                                 Type="Syncfusion.Blazor.InPlaceEditor.InputType.Text"
                                 @bind-Value="@Organization.Name"
                                 Mode="@RenderMode.Inline"
                                 TValue="string"
                                 Name="Name"
                                 EmptyText="Enter the Organization name">
                    <EditorComponent>
                        <EditForm Model="@Organization">
                            <SfTextBox @bind-Value="@Organization.Name" Placeholder="Enter the Organization name"></SfTextBox>
                            <ValidationMessage For=@(() => Organization.Name) />
                        </EditForm>
                    </EditorComponent>
                    <InPlaceEditorPopupSettings Width="auto"></InPlaceEditorPopupSettings>
                </SfInPlaceEditor>
            </div>*@

            @*<a class="btn btn-sm" @onclick="ToggleEditName">
            <span class="oi oi-pencil" aria-hidden="true"></span>
        </a>*@
            @*<div class="form-group row">
            <label for="address" class="col-sm-3">Address: </label>
            <InputText id="address" class="form-control col-sm-8" @bind-Value="@Organization.Address.AddressLine" placeholder="Enter last name"></InputText>
        </div>*@



            <div class="d-flex justify-content-end">
                <div class="d-flex pr-2">
                    <button type="submit"
                            class="btn btn-primary edit-btn"
                            @onclick="ToggleFormMode">
                        <span class="oi oi-check" aria-hidden="true"></span>
                    </button>
                </div>
                <div>
                    <a class="btn btn-danger" @onclick="DeleteOrganization">
                        <span class="oi oi-trash" aria-hidden="true"></span>
                    </a>
                </div>
            </div>
            @if (Saved)
            {
                <div class="alert @StatusClass">@Message</div>
            }
        </EditForm>
    </section>
}