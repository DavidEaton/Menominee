@typeparam TItem

<table class="table">
    <thead>
        <tr class="small">
            @TableHeader
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Items)
        {
            <tr @key="item">
                @RowTemplate(item)
            </tr>
        }
    </tbody>
</table>